//here we will write the places data model class

class PlacesModel {
  String? name;
  String? location;
  String? description;
  int? price;

  PlacesModel(this.name, this.location, this.description, this.price);
}
